# Generated artifacts — regenerate with `make` (which runs `make gen` +
# `make extract`). Do NOT check these in; they are reproducible from the
# fixture + skill handoff graph via tools/contract-tests/src/bin/gen_coq.rs
# and `coqc Extraction.v`.

# Emitted by `make gen` (gen_coq.rs, from Repo::from_root):
theories/Generated.v
extraction/checker.ml

# Emitted by `make extract` (coqc Extraction.v):
extraction/forge_state_machine.ml
extraction/forge_state_machine.mli
test/forge_state_machine.ml
test/forge_state_machine.mli

# Dune / Coq build artifacts:
_build/
*.vo
*.vok
*.vos
*.glob
*.aux
.lia.cache
dune-workspace
# Opam file generated by dune (generate_opam_files true in dune-project):
forge-contract-proofs.opam
